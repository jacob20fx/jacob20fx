{
  "name": "jacob20fx-nda-system",
  "version": "3.0.0",
  "private": true,
  "description": "Stripe payment verification, electronic NDA and Discord access on Vercel.",
  "scripts": {
    "test": "node tests/run-tests.js",
    "check": "node --check api/_helpers.js && node --check api/verify-payment.js && node --check api/sign-nda.js && node --check api/health.js && node --check nda.js && node --check app.js && node --check admin.js"
  },
  "engines": { "node": ">=20" }
}
